"use strict";
/*
 * ATTENTION: An "eval-source-map" devtool has been used.
 * This devtool is neither made for production nor for readable output files.
 * It uses "eval()" calls to create a separate source file with attached SourceMaps in the browser devtools.
 * If you are trying to read the output file, select a different devtool (https://webpack.js.org/configuration/devtool/)
 * or disable the default devtool with "devtool: false".
 * If you are looking for production-ready output files, see mode: "production" (https://webpack.js.org/configuration/mode/).
 */
self["webpackHotUpdate_N_E"]("_app-pages-browser_packages_core_src_index_ts",{

/***/ "(app-pages-browser)/../../packages/core/src/components/instagram-overlay/shader.vert.glsl":
/*!*****************************************************************************!*\
  !*** ../../packages/core/src/components/instagram-overlay/shader.vert.glsl ***!
  \*****************************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

module.exports = "// ═══════════════════════════════════════════════════════════════════════════\n// Voronoi Glass Shatter — Vertex Shader\n// ═══════════════════════════════════════════════════════════════════════════\n// Simulates physical air resistance (friction drag) during particle scatter\n// and a damped harmonic spring oscillator with overshoot for organic reassembly.\n// ═══════════════════════════════════════════════════════════════════════════\n\nuniform float uTime;\nuniform float uHover;\nuniform vec2  uMouse;\nuniform vec2  uClickPos;\nuniform float uClickTime;\n\nvarying vec2 vUv;\nvarying float vClickAmt;\nvarying float vRebuildProgress;\nvarying float vShattered;\n\n#define GRID_SIZE 72.0\n#define FRICTION 3.8\n#define SPRING_FREQ 8.0\n#define SPRING_DAMP 2.4\n\n// Procedural Hash Utilities\nfloat hash(vec2 p) {\n  return fract(sin(dot(p, vec2(127.1, 311.7))) * 43758.5453123);\n}\n\nvec2 hash22(vec2 p) {\n  p = vec2(dot(p, vec2(127.1, 311.7)), dot(p, vec2(269.5, 183.3)));\n  return fract(sin(p) * 43758.5453123);\n}\n\nvoid main() {\n  vec2 currentUv = uv;\n  vec3 displacedPos = position;\n\n  // ─── Click Timeline Analysis ─────────────────────────────────────────────\n  float timeSinceClick = uTime - uClickTime;\n  \n  if (timeSinceClick > 0.0 && timeSinceClick < 4.2) {\n    vShattered = 1.0;\n    \n    // Snap uv to cell center grid coordinate\n    vec2 C = (floor(currentUv * GRID_SIZE) + 0.5) / GRID_SIZE;\n    vec2 localPos = currentUv - C;\n    \n    // ─── 1. Calculate Initial Velocity ─────────────────────────────────────\n    float distToClick = distance(C, uClickPos);\n    vec2 blastDir = normalize(C - uClickPos);\n    float blastForce = exp(-distToClick * 2.8) * 1.6; // Concentrated center force\n    \n    vec2 randDir = hash22(C) - 0.5;\n    vec2 V_0 = blastDir * blastForce + randDir * 0.45;\n    \n    // ─── 2. Scatter Phase with Air Friction Drag (t = 0.0 to 2.0s) ──────────\n    float tFall = clamp(timeSinceClick, 0.0, 2.0);\n    float dragFactor = (1.0 - exp(-FRICTION * tFall)) / FRICTION;\n    \n    vec2 fallOffset = V_0 * dragFactor * 0.08;\n    float fallZ = (hash(C) * 0.16 + 0.06) * dragFactor;\n    \n    // ─── 3. Reassembly Phase with Damped Spring Overshoot (t > 2.0s) ────────\n    float tRebuild = timeSinceClick - 2.0;\n    vec2 currentOffset = fallOffset;\n    float currentZ = fallZ;\n    float rebuildProgress = 0.0;\n    \n    if (tRebuild > 0.0) {\n      // Damped harmonic oscillator decay factor\n      float springOscillation = cos(SPRING_FREQ * tRebuild) * exp(-SPRING_DAMP * tRebuild);\n      currentOffset = fallOffset * springOscillation;\n      currentZ = fallZ * springOscillation;\n      rebuildProgress = smoothstep(0.0, 1.4, tRebuild);\n    }\n    \n    vClickAmt = 1.0 - rebuildProgress;\n    vRebuildProgress = rebuildProgress;\n\n    // ─── 4. Particle Shrink and Growth scale ───────────────────────────────\n    float scale = mix(1.0, 0.18, smoothstep(0.0, 0.4, timeSinceClick));\n    if (tRebuild > 0.0) {\n      scale = mix(0.18, 1.0, smoothstep(0.0, 1.1, tRebuild));\n    }\n    \n    // ─── 5. Final Coordinates ──────────────────────────────────────────────\n    vec2 finalUv = C + localPos * scale + currentOffset;\n    \n    displacedPos.xy = finalUv - 0.5;\n    displacedPos.z += currentZ;\n    \n    vUv = finalUv;\n  } else {\n    // Idle / Hover\n    vUv = currentUv;\n    vClickAmt = 0.0;\n    vRebuildProgress = 0.0;\n    vShattered = 0.0;\n  }\n\n  gl_Position = projectionMatrix * modelViewMatrix * vec4(displacedPos, 1.0);\n}";

/***/ })

});