"use strict";
/*
 * ATTENTION: An "eval-source-map" devtool has been used.
 * This devtool is neither made for production nor for readable output files.
 * It uses "eval()" calls to create a separate source file with attached SourceMaps in the browser devtools.
 * If you are trying to read the output file, select a different devtool (https://webpack.js.org/configuration/devtool/)
 * or disable the default devtool with "devtool: false".
 * If you are looking for production-ready output files, see mode: "production" (https://webpack.js.org/configuration/mode/).
 */
self["webpackHotUpdate_N_E"]("_app-pages-browser_packages_core_src_index_ts",{

/***/ "(app-pages-browser)/../../packages/core/src/components/instagram-overlay/shader.vert.glsl":
/*!*****************************************************************************!*\
  !*** ../../packages/core/src/components/instagram-overlay/shader.vert.glsl ***!
  \*****************************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

module.exports = "// ═══════════════════════════════════════════════════════════════════════════\n// Voronoi Glass Shatter — Vertex Shader\n// ═══════════════════════════════════════════════════════════════════════════\n// Performs dynamic 3D shard push-out and rotational spring physics using a\n// high-density jittered grid Voronoi structure with organic noise perturbation.\n// ═══════════════════════════════════════════════════════════════════════════\n\nuniform float uTime;\nuniform float uHover;\nuniform vec2  uMouse;\nuniform vec2  uClickPos;\nuniform float uClickTime;\n\nvarying vec2 vUv;\nvarying float vClickAmt;\nvarying float vBorderIntensity;\n\n// ─── Pseudo-Random Noise Utilities ────────────────────────────────────────\nfloat hash(vec2 p) {\n  return fract(sin(dot(p, vec2(127.1, 311.7))) * 43758.5453123);\n}\n\nvec2 hash22(vec2 p) {\n  p = vec2(dot(p, vec2(127.1, 311.7)), dot(p, vec2(269.5, 183.3)));\n  return fract(sin(p) * 43758.5453123);\n}\n\nfloat noise(vec2 p) {\n  vec2 i = floor(p);\n  vec2 f = fract(p);\n  f = f * f * (3.0 - 2.0 * f);\n  return mix(mix(hash(i), hash(i + vec2(1.0, 0.0)), f.x),\n             mix(hash(i + vec2(0.0, 1.0)), hash(i + vec2(1.0, 1.0)), f.x), f.y);\n}\n\nfloat fbm(vec2 p) {\n  return noise(p) * 0.6 + noise(p * 2.5) * 0.4;\n}\n\n// ─── Jittered Grid Voronoi ────────────────────────────────────────────────\nvoid getVoronoiData(vec2 uv, out vec2 center, out float cellId) {\n  vec2 ip = floor(uv * 7.0); // 7x7 grid density ≈ 49 potential cell domains\n  vec2 fp = fract(uv * 7.0);\n  \n  float minDist = 9.0;\n  vec2 closestPointLocal;\n  vec2 closestPointGrid;\n  \n  for (int y = -1; y <= 1; y++) {\n    for (int x = -1; x <= 1; x++) {\n      vec2 g = vec2(float(x), float(y));\n      // Cell center jittered procedurally\n      vec2 point = g + hash22(ip + g);\n      float d = distance(fp, point);\n      if (d < minDist) {\n        minDist = d;\n        closestPointLocal = point;\n        closestPointGrid = g;\n      }\n    }\n  }\n  \n  center = (ip + closestPointLocal) / 7.0;\n  cellId = hash(ip + closestPointGrid);\n}\n\nvoid main() {\n  vec2 currentUv = uv;\n  vec3 displacedPos = position;\n\n  // ─── Click Progress Decay ────────────────────────────────────────────────\n  float timeSinceClick = uTime - uClickTime;\n  float clickAmt = 0.0;\n  \n  if (timeSinceClick > 0.0 && timeSinceClick < 3.0) {\n    // Sharp spring-back explosive release\n    clickAmt = clamp(timeSinceClick * 10.0, 0.0, 1.0) * exp(-timeSinceClick * 2.2);\n  }\n  \n  vClickAmt = clickAmt;\n\n  if (clickAmt > 0.001) {\n    // Add FBM noise to Voronoi query to generate organic jagged borders\n    vec2 jaggedUv = currentUv + vec2(\n      fbm(currentUv * 16.0) * 0.006,\n      fbm(currentUv * 16.0 + vec2(1.0)) * 0.006\n    );\n    \n    vec2 center;\n    float cellId;\n    getVoronoiData(jaggedUv, center, cellId);\n\n    float distToClick = distance(center, uClickPos);\n    float force = clickAmt * exp(-distToClick * 3.0);\n    \n    // 3D displacement vectors\n    vec2 displaceDir = normalize(center - uClickPos);\n    vec2 cellOffset = displaceDir * force * 0.09;\n    float zOffset = force * 0.11;\n    \n    // Rotational spring physics\n    float rotAngle = sin(cellId * 32.8) * force * 0.32;\n    vec2 localPos = currentUv - center;\n    float s = sin(rotAngle);\n    float c = cos(rotAngle);\n    vec2 rotatedLocal = vec2(\n      localPos.x * c - localPos.y * s,\n      localPos.x * s + localPos.y * c\n    );\n    \n    vec2 finalUv = center + rotatedLocal + cellOffset;\n    \n    displacedPos.xy = finalUv - 0.5;\n    displacedPos.z += zOffset;\n    \n    vUv = finalUv;\n    vBorderIntensity = force;\n  } else {\n    vUv = currentUv;\n    vBorderIntensity = 0.0;\n  }\n\n  gl_Position = projectionMatrix * modelViewMatrix * vec4(displacedPos, 1.0);\n}";

/***/ })

});