"use client";  import React, { useRef, useState, useEffect } from "react"; import gsap from "gsap"; import { ScrollTrigger } from "gsap/ScrollTrigger";  if (typeof window !== "undefined") {   gsap.registerPlugin(ScrollTrigger); }  export default function Hero() {   const containerRef = useRef<HTMLDivElement>(null);   const gridRef = useRef<SVGSVGElement>(null);   const textContainerRef = useRef<HTMLDivElement>(null);   const [copied, setCopied] = useState(false);    // Refs for mouse-interactive inner nodes   const frag1Ref = useRef<HTMLDivElement>(null);   const frag2Ref = useRef<HTMLDivElement>(null);   const frag3Ref = useRef<HTMLDivElement>(null);   const frag4Ref = useRef<HTMLDivElement>(null);   const frag5Ref = useRef<HTMLDivElement>(null);    // Refs for scroll-parallax outer nodes   const frag1ScrollRef = useRef<HTMLDivElement>(null);   const frag2ScrollRef = useRef<HTMLDivElement>(null);   const frag3ScrollRef = useRef<HTMLDivElement>(null);   const frag4ScrollRef = useRef<HTMLDivElement>(null);   const frag5ScrollRef = useRef<HTMLDivElement>(null);    // Command copy handler   const handleCopy = async () => {     try {       await navigator.clipboard.writeText("npx absoluteui init");       setCopied(true);       setTimeout(() => setCopied(false), 2000);     } catch (err) {       console.error("Failed to copy text: ", err);     }   };    // SVG Grid & Parallax Animations   useEffect(() => {     const container = containerRef.current;     const svg = gridRef.current;     const textContainer = textContainerRef.current;     if (!container || !svg) return;      const width = container.clientWidth;     const height = container.clientHeight;          // Grid parameters     const spacing = 80;     const vCount = Math.floor(width / spacing);     const hCount = Math.floor(height / spacing);      // Create vertical lines     const vLines: { x: number; el: SVGPathElement }[] = [];     for (let i = 1; i <= vCount; i++) {       const x = i * spacing;       const path = document.createElementNS("http://www.w3.org/2000/svg", "path");       path.setAttribute("class", "stroke-border-subtle/30");       path.setAttribute("stroke-width", "1");       path.setAttribute("fill", "none");       path.setAttribute("d", `M ${x} 0 L ${x} ${height}`);       svg.appendChild(path);       vLines.push({ x, el: path });     }      // Create horizontal lines     const hLines: { y: number; el: SVGPathElement }[] = [];     for (let i = 1; i <= hCount; i++) {       const y = i * spacing;       const path = document.createElementNS("http://www.w3.org/2000/svg", "path");       path.setAttribute("class", "stroke-border-subtle/30");       path.setAttribute("stroke-width", "1");       path.setAttribute("fill", "none");       path.setAttribute("d", `M 0 ${y} L ${width} ${y}`);       svg.appendChild(path);       hLines.push({ y, el: path });     }      // Animation constants     const maxDist = 250;     const pullStrength = 0.45;      // Subtle automatic grid drift (Layer 1)     const driftTimeline = gsap.timeline({ repeat: -1, yoyo: true });     driftTimeline.to(svg, {       x: 10,       y: 10,       duration: 12,       ease: "sine.inOut",     });      // 1. Scroll-driven parallax for typography (Layer 5 scroll)     if (textContainer) {       gsap.to(textContainer, {         y: 130,         opacity: 0.15,         ease: "none",         scrollTrigger: {           trigger: container,           start: "top top",           end: "bottom top",           scrub: true,         },       });     }      // 2. Scroll-driven parallax for floating components (Layer 3 scroll)     const initScrollParallax = (el: HTMLDivElement | null, speed: number) => {       if (!el) return;       gsap.to(el, {         y: speed,         ease: "none",         scrollTrigger: {           trigger: container,           start: "top top",           end: "bottom top",           scrub: true,         },       });     };      initScrollParallax(frag1ScrollRef.current, -120);     initScrollParallax(frag2ScrollRef.current, -200);     initScrollParallax(frag3ScrollRef.current, -70);     initScrollParallax(frag4ScrollRef.current, -240);     initScrollParallax(frag5ScrollRef.current, -150);      // 3. Mouse-driven interactive motion system (Layer 4 mouse)     const handleMouseMove = (e: MouseEvent) => {       const rect = container.getBoundingClientRect();       const mouseX = e.clientX - rect.left;       const mouseY = e.clientY - rect.top;        // Warp SVG Grid lines       vLines.forEach((line) => {         const distX = mouseX - line.x;         const distY = Math.abs(mouseY - (rect.height / 2));         const dist = Math.sqrt(distX * distX + distY * distY);          if (dist < maxDist) {           const factor = (1 - dist / maxDist) * pullStrength;           const targetX = line.x + distX * factor;           gsap.to(line.el, {             attr: { d: `M ${line.x} 0 Q ${targetX} ${mouseY} ${line.x} ${height}` },             duration: 0.5,             ease: "power2.out",             overwrite: "auto",           });         } else {           gsap.to(line.el, {             attr: { d: `M ${line.x} 0 L ${line.x} ${height}` },             duration: 0.9,             ease: "power2.out",             overwrite: "auto",           });         }       });        hLines.forEach((line) => {         const distY = mouseY - line.y;         const distX = Math.abs(mouseX - (rect.width / 2));         const dist = Math.sqrt(distX * distX + distY * distY);          if (dist < maxDist) {           const factor = (1 - dist / maxDist) * pullStrength;           const targetY = line.y + distY * factor;           gsap.to(line.el, {             attr: { d: `M 0 ${line.y} Q ${mouseX} ${targetY} ${width} ${line.y}` },             duration: 0.5,             ease: "power2.out",             overwrite: "auto",           });         } else {           gsap.to(line.el, {             attr: { d: `M 0 ${line.y} L ${width} ${line.y}` },             duration: 0.9,             ease: "power2.out",             overwrite: "auto",           });         }       });        // Pointer interactive tilt for fragments       const moveFrag = (el: HTMLDivElement | null, weight: number) => {         if (!el) return;         const deltaX = (e.clientX - window.innerWidth / 2) * weight;         const deltaY = (e.clientY - window.innerHeight / 2) * weight;         gsap.to(el, {           x: deltaX,           y: deltaY,           rotationX: -deltaY * 0.05,           rotationY: deltaX * 0.05,           duration: 0.8,           ease: "power2.out",           overwrite: "auto",         });       };        moveFrag(frag1Ref.current, -0.04);       moveFrag(frag2Ref.current, 0.03);       moveFrag(frag3Ref.current, -0.02);       moveFrag(frag4Ref.current, 0.05);       moveFrag(frag5Ref.current, -0.03);     };      const handleMouseLeave = () => {       // Reset grid paths       vLines.forEach((line) => {         gsap.to(line.el, {           attr: { d: `M ${line.x} 0 L ${line.x} ${height}` },           duration: 0.9,           ease: "power3.out",           overwrite: "auto",         });       });       hLines.forEach((line) => {         gsap.to(line.el, {           attr: { d: `M 0 ${line.y} L ${width} ${line.y}` },           duration: 0.9,           ease: "power3.out",           overwrite: "auto",         });       });        // Reset fragments mouse offset       const resetFrag = (el: HTMLDivElement | null) => {         if (!el) return;         gsap.to(el, {           x: 0,           y: 0,           rotationX: 0,           rotationY: 0,           duration: 1.2,           ease: "power3.out",           overwrite: "auto",         });       };       resetFrag(frag1Ref.current);       resetFrag(frag2Ref.current);       resetFrag(frag3Ref.current);       resetFrag(frag4Ref.current);       resetFrag(frag5Ref.current);     };      container.addEventListener("mousemove", handleMouseMove);     container.addEventListener("mouseleave", handleMouseLeave);      return () => {       container.removeEventListener("mousemove", handleMouseMove);       container.removeEventListener("mouseleave", handleMouseLeave);       driftTimeline.kill();       svg.innerHTML = "";     };   }, []);    return (     <section       ref={containerRef}       className="relative flex h-screen w-full flex-col items-center justify-center bg-bg-base px-6 text-center overflow-hidden select-none border-b border-border-subtle"       style={{ perspective: "1000px" }}     >       {/* Layer 1: Subtle moving grid system */}       <svg         ref={gridRef}         className="absolute inset-0 -z-10 h-full w-full pointer-events-none opacity-80"       />        {/* Layer 2: Noise + atmospheric depth */}       <div className="absolute inset-0 -z-20 pointer-events-none opacity-[0.018] bg-[url('data:image/svg+xml;utf8,<svg viewBox=%220 0 200 200%22 xmlns=%22http://www.w3.org/2000/svg%22><filter id=%22noise%22><feTurbulence type=%22fractalNoise%22 baseFrequency=%220.85%22 numOctaves=%223%22 stitchTiles=%22stitch%22/></filter><rect width=%22100%25%22 height=%22100%25%22 filter=%22url(%23noise)%22/></svg>')] bg-repeat" />       <div className="absolute top-1/4 left-1/4 -z-30 h-[450px] w-[450px] rounded-full bg-accent-subtle/30 blur-[130px] animate-[pulse_10s_infinite_alternate]" />       <div className="absolute bottom-1/4 right-1/4 -z-30 h-[550px] w-[550px] rounded-full bg-accent-subtle/25 blur-[150px] animate-[pulse_12s_infinite_alternate_2s]" />        {/* Layer 3: Floating component fragments with nested outer scroll refs */}              {/* Fragment 1: Minimal Button Outline */}       <div         ref={frag1ScrollRef}         className="absolute top-[20%] left-[12%] z-10 hidden md:block pointer-events-none select-none"       >         <div           ref={frag1Ref}           className="flex items-center gap-2 border border-border-clean bg-bg-surface/50 backdrop-blur-md px-3.5 py-1.5 rounded-sm shadow-sm animate-[bounce_8s_infinite_ease-in-out]"         >           <span className="text-[9px] uppercase tracking-wider font-semibold text-fg-secondary">Primitive</span>           <div className="w-1.5 h-1.5 rounded-full bg-accent" />         </div>       </div>        {/* Fragment 2: Custom Active Toggle */}       <div         ref={frag2ScrollRef}         className="absolute top-[25%] right-[15%] z-10 hidden md:block pointer-events-none select-none"       >         <div           ref={frag2Ref}           className="flex items-center gap-4 border border-border-clean bg-bg-surface/50 backdrop-blur-md px-4 py-2 rounded-sm shadow-sm animate-[bounce_10s_infinite_ease-in-out_1s]"         >           <span className="text-[8px] uppercase tracking-wider font-bold text-fg-muted">System Active</span>           <div className="w-7 h-4 bg-accent/20 border border-accent/40 rounded-full relative">             <div className="absolute right-0.5 top-0.5 w-2.5 h-2.5 rounded-full bg-accent" />           </div>         </div>       </div>        {/* Fragment 3: Code Editor Bracket */}       <div         ref={frag3ScrollRef}         className="absolute bottom-[28%] left-[15%] z-10 hidden md:block pointer-events-none select-none"       >         <div           ref={frag3Ref}           className="flex flex-col gap-1.5 border border-border-clean bg-bg-surface/50 backdrop-blur-md p-3 rounded-sm shadow-sm w-36 animate-[bounce_9s_infinite_ease-in-out_2s]"         >           <div className="flex items-center gap-1.5 border-b border-border-subtle pb-1">             <div className="w-1.5 h-1.5 rounded-full bg-fg-muted" />             <span className="text-[7.5px] font-mono text-fg-muted">tokens.json</span>           </div>           <div className="h-1 bg-border-clean rounded-full w-4/5" />           <div className="h-1 bg-accent/30 rounded-full w-1/2" />         </div>       </div>        {/* Fragment 4: Slider Track */}       <div         ref={frag4ScrollRef}         className="absolute bottom-[22%] right-[12%] z-10 hidden md:block pointer-events-none select-none"       >         <div           ref={frag4Ref}           className="flex flex-col gap-2 border border-border-clean bg-bg-surface/50 backdrop-blur-md p-3.5 rounded-sm shadow-sm w-44 animate-[bounce_11s_infinite_ease-in-out_0.5s]"         >           <div className="flex justify-between items-center text-[8px] uppercase font-bold text-fg-secondary">             <span>Velocity</span>             <span className="text-accent">45%</span>           </div>           <div className="h-1 bg-border-subtle rounded-full w-full relative">             <div className="absolute left-[45%] top-1/2 -translate-y-1/2 w-2.5 h-2.5 rounded-full bg-accent border border-bg-base" />           </div>         </div>       </div>        {/* Fragment 5: Minimal Card Node */}       <div         ref={frag5ScrollRef}         className="absolute top-[48%] right-[8%] z-10 hidden lg:block pointer-events-none select-none"       >         <div           ref={frag5Ref}           className="flex items-center gap-3 border border-border-clean bg-bg-surface/40 backdrop-blur-sm px-4 py-2 rounded-sm animate-[bounce_7s_infinite_ease-in-out_1.5s]"         >           <div className="w-3 h-3 rounded-sm border border-accent bg-accent/10" />           <div className="flex flex-col gap-1">             <div className="h-1 bg-fg-secondary w-12 rounded-full" />             <div className="h-0.5 bg-border-clean w-8 rounded-full" />           </div>         </div>       </div>        {/* Layer 5: Typography (with scroll wrapper) */}       <div         ref={textContainerRef}         className="mx-auto flex max-w-5xl flex-col items-center justify-center gap-6 z-20"       >         <h1 className="font-sans text-5xl font-black leading-none tracking-[-0.04em] text-fg-primary md:text-8xl select-none">           BEAUTY FIRST.           <br />           MOTION DRIVEN.         </h1>          <p className="max-w-xl font-sans text-xs md:text-sm font-medium tracking-tight text-fg-secondary leading-relaxed opacity-90 select-none">           A living digital exhibition of premium React component craft. Experience interfaces that react, evolve, and coordinate.         </p>          {/* Action CTAs */}         <div className="mt-8 flex flex-col items-center justify-center gap-4 sm:flex-row">           <a             href="#exhibition"             className="group relative flex h-11 items-center justify-center border border-border-clean bg-fg-primary px-6 rounded-sm font-sans text-xs font-bold uppercase tracking-wider text-bg-base hover:bg-fg-secondary hover:border-fg-secondary transition-all duration-200 cursor-pointer"           >             Enter Exhibition           </a>            <div             onClick={handleCopy}             className="group flex h-11 cursor-pointer items-center justify-between gap-4 border border-border-subtle bg-bg-surface hover:bg-bg-elevated px-4 rounded-sm font-mono text-xs text-fg-secondary hover:text-fg-primary transition-all duration-150 select-none"           >             <span>npx absoluteui init</span>             <button className="font-sans text-[10px] font-bold uppercase tracking-wider text-fg-muted group-hover:text-fg-primary transition-colors focus:outline-none cursor-pointer">               {copied ? (                 <span className="text-accent font-bold">copied!</span>               ) : (                 <span>copy</span>               )}             </button>           </div>         </div>       </div>     </section>   ); }